apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "keyless-signature.fullname" . }}-test-connection"
  labels:
    {{- include "keyless-signature.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "keyless-signature.fullname" . }}:{{ .Values.ctfe.service.port }}']
  restartPolicy: Never
