apiVersion: v1
kind: Service
metadata:
  name: {{ include "keyless-signature.fullname" . }}-ctfe
  labels:
    {{- include "keyless-signature.labels" . | nindent 4 }}
spec:
  type: {{ .Values.ctfe.service.type }}
  ports:
    - port: {{ .Values.ctfe.service.port }}
      targetPort: {{ .Values.ctfe.service.port }}
      protocol: TCP
  selector:
    {{- include "keyless-signature.selectorLabels" . | nindent 4 }}
    app: ctfe
